/* Dedicated real-solver route using the same mobile-first visual system as the approved homepage. */
import Link from "next/link";
import SiteHeader from "@/components/SiteHeader";
import ManualSolver from "@/components/ManualSolver";
export default function SolverPage(){return <main className="app-shell relative min-h-dvh w-full max-w-[460px] overflow-hidden px-5 pb-[calc(28px+env(safe-area-inset-bottom))] pt-[22px]"><div className="orb orb-a"/><div className="orb orb-b"/><div className="relative z-[1]"><SiteHeader/><Link href="/" className="mt-4 inline-flex text-sm font-bold text-[var(--muted)]">← Back to home</Link><section className="mt-5"><p className="text-xs font-extrabold tracking-[.18em] text-[var(--green)]">REAL 3×3 SOLVER</p><h1 className="mt-2 text-[38px] font-extrabold leading-[1.02] tracking-[-1px]">Scramble. Solve.<br/><span className="accent-text">Verify every move.</span></h1><p className="mt-3 text-[15px] text-[var(--muted)]">Run a random scramble, or tap in your own cube's colors — the camera scanner comes last.</p></section><div className="mt-5"><ManualSolver/></div></div></main>}
