import ProfilePlaceholderPage from "@/components/ProfilePlaceholderPage";

export default function ProfileCollectionPage() {
  return (
    <ProfilePlaceholderPage
      eyebrow="Cube Shelf"
      title="My Cubes"
      description="The approved version will manage owned cubes, favorites, photos, main puzzle selection, and public collection privacy."
      primaryHref="/profile"
      primaryLabel="Back to dashboard"
    />
  );
}
