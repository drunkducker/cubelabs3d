import ProfilePlaceholderPage from "@/components/ProfilePlaceholderPage";

export default function ProfileAchievementsPage() {
  return (
    <ProfilePlaceholderPage
      eyebrow="Achievements"
      title="Badge Case"
      description="The approved version will show unlocked badges, progress paths, XP rewards, streak badges, and collection achievements."
      primaryHref="/leaderboard"
      primaryLabel="View leaderboard"
    />
  );
}
